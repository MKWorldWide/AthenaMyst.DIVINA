""
Main entry point for the backtest CLI.

This module allows running the backtest CLI directly with `python -m divina.backtest`.
"""
from divina.backtest.cli import app

if __name__ == "__main__":
    app()
